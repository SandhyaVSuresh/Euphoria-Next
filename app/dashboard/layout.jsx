export default function DashboardLayout({children}){
    return(
        <section>
            <h1>This is nav</h1>
            {children}
            <h1> this is footer</h1>
        </section>
    )
}